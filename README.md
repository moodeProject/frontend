# Safe Helmet UI v17

Changes from v16:
- Worker status page uses a 4-column desktop grid so cards are not overly wide.
- Responsive grid: 4 columns -> 3 columns -> 2 columns -> 1 column.
- Keeps all v16 features intact.


## v18 update
- Worker status cards now use a fixed 260px width.
- Desktop shows up to 4 fixed-size cards per row without stretching.
